package com.buttersus.regex

/**
 * A class that represents a log of the parser.
 * - It provides a way to get a list of all nodes that were created during the parsing.
 * - Information about transitions between nodes is also available.
 * - Enable CLI mode to see the log in the console.
 */
class ParserLog {
}